<UserControl x:Class="AppricationViewModule.Views.ReserveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:tp="clr-namespace:CustomControlLibrary.TimePicker;assembly=CustomControlLibrary"
             xmlns:cb="clr-namespace:CustomControlLibrary.Button;assembly=CustomControlLibrary"
             xmlns:lc="clr-namespace:CustomControlLibrary.Text;assembly=CustomControlLibrary"
             xmlns:bc="clr-namespace:CustomControlLibrary.CustomBaseControl;assembly=CustomControlLibrary"
             xmlns:local="clr-namespace:AppricationViewModule.Views"
             mc:Ignorable="av"
             prism:ViewModelLocator.AutoWireViewModel="True"
             av:DesignWidth="644.089" av:DesignHeight="316.832">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <lc:LabelingContainer Text="START" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <DatePicker SelectedDate="{Binding StartDate}" VerticalAlignment="Center"/>
                    <tp:TimePicker SelectedTime="{Binding StartTime}" VerticalAlignment="Center" Width="60"/>
                </StackPanel>
            </lc:LabelingContainer>
            <lc:LabelingContainer Text="END" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <DatePicker SelectedDate="{Binding EndDate}" VerticalAlignment="Center"/>
                    <tp:TimePicker SelectedTime="{Binding EndTime}" VerticalAlignment="Center" Width="60"/>
                </StackPanel>
            </lc:LabelingContainer>
            <lc:LabelingContainer Text="" Margin="0,0,10,0">
                <cb:CommandButton Command="{Binding CreateCommand}" Text="CREATE" VerticalAlignment="Center" Height="24" Width="100"/>
            </lc:LabelingContainer>
        </StackPanel>
        <ContentControl Grid.Row="1">
            <ScrollViewer Background="Beige" VerticalScrollBarVisibility="Auto">
                <bc:BaseTextBoxControl />
            </ScrollViewer>
        </ContentControl>
        <ContentControl Grid.Row="2">
            <bc:BaseScrollViewerControl Background="GhostWhite" VerticalScrollBarVisibility="Auto">
                <bc:BaseTextBoxControl/>
            </bc:BaseScrollViewerControl>
        </ContentControl>
    </Grid>
</UserControl>
